{
  "name": "mofa-podcast",
  "version": "0.4.7",
  "author": "hagency",
  "description": "Multi-speaker podcast generation with TTS voice synthesis and audio assembly",
  "timeout_secs": 1800,
  "requires_network": true,
  "requires": {
    "bins": ["ominix-api"],
    "os": ["darwin"]
  },
  "discovery": {
    "summary": "Multi-speaker podcast generator: review-then-render scripts to a final MP3 with built-in or cloned TTS voices.",
    "hints": [
      {
        "when": "authoring or repairing the podcast script (format, dialogue lines, Chinese examples)",
        "read": "docs/script-format.md"
      },
      {
        "when": "picking an emotion tag for a dialogue line",
        "read": "docs/emotion-tags.md"
      },
      {
        "when": "inserting BGM or [PAUSE] cues (including Chinese variants)",
        "read": "docs/cues.md"
      },
      {
        "when": "choosing voices or working with cloned voices",
        "read": "docs/voices.md"
      },
      {
        "when": "understanding the generation pipeline or output paths",
        "read": "docs/generation-engine.md"
      }
    ]
  },
  "skill_md_auto_inject": false,
  "tools": [
    {
      "name": "podcast_voices",
      "description": "List all available voices for podcast speakers. Shows preset (built-in) voices and any saved custom/cloned voices. Call this only when the user explicitly asks to browse or choose voices; do not call it as a precheck before podcast_generate.",
      "input_schema": {
        "type": "object",
        "properties": {}
      }
    },
    {
      "name": "podcast_generate",
      "spawn_only": true,
      "spawn_only_message": "SUCCESS: Podcast generation is now running in background. Voice segments are being generated and will be assembled into the final audio file. The audio file will be automatically delivered when ready.",
      "description": "Generate the full podcast audio from an approved markdown script. Validates and deterministically repairs common markdown drift such as bold-wrapped speaker headers, multiline dialogue blocks, and full-width header punctuation before parsing. Then generates TTS for each segment (built-in voices first, then cloned to minimize model switching), concatenates in timeline order with pauses, and outputs a single final audio file. This runs in background — do NOT wait for it.",
      "input_schema": {
        "type": "object",
        "properties": {
          "script": {
            "type": "string",
            "description": "The full markdown podcast script content. Canonical dialogue format is [Character - voice, emotion] text, but the validator can repair common markdown formatting drift before synthesis."
          },
          "script_path": {
            "type": "string",
            "description": "Path to the markdown script file. Alternative to providing script content directly."
          },
          "output_dir": {
            "type": "string",
            "description": "Directory for output files. Defaults to skill-output/mofa-podcast/"
          }
        }
      }
    }
  ]
}
